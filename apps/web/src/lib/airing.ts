/** Shared airing helpers — re-export from `@baykus/ui` (strangler Phase 4.7). */
export {
  type AiringFields,
  airInstantIso,
  formatAirStampLocal,
  formatAirStampOrigin,
  isEpisodeAired,
  msUntilAir,
  normalizeAirStamp,
  todayIso,
} from "@baykus/ui/lib/airing";
