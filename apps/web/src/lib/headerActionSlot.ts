/**
 * DOM id for the mobile app header's series-detail action slot (E155-style
 * pattern). Layout.tsx renders the empty target element; SeriesDetailPage
 * portals its overflow-menu trigger into it so the button lives in the fixed
 * header rather than the scrolling hero on mobile.
 */
export const SERIES_HEADER_ACTION_SLOT_ID = "series-header-action";
