/**
 * CommonJS mirror of `tokens.ts` for `tailwind.config.js` (Node require).
 * Keep hex values identical to `src/tokens.ts` / web `index.css` `@theme`.
 */
module.exports = {
  colors: {
    void: "#080808",
    snow: "#ebebeb",
    muted: "#888888",
    "muted-dim": "#848484",
    yellow: "#f0e000",
  },
  fonts: {
    display: ["DM Serif Display", "serif"],
    sans: ["DM Sans", "sans-serif"],
    mono: ["JetBrains Mono", "monospace"],
  },
};
